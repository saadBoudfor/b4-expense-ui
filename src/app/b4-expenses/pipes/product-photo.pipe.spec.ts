import { ProductPhotoPipe } from './product-photo.pipe';

describe('ProductPhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
