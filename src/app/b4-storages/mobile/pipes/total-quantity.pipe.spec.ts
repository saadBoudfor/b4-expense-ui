import { TotalQuantityPipe } from './total-quantity.pipe';

describe('TotalQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
